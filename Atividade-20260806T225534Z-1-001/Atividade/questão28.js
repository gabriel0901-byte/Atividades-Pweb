let idade = 16;
console.log(idade);
// a Variável definida está escrita com letra maíuscula e no log estava com letra minúscula

const nota = 8;
if (nota >= 7) {
console.log(Aprovado);
}
// a nota não é igual a 7, deve ser escrita como >= ou todos diferentes serão errados

const idade = 20;
if (idade >= 18) {
console.log("Pode entrar")};
//estava faltando a chave fechada após o console.log

const nome = "Ana";
console.log(nome)
// não possui nenhum erro

const preço = 10;
console.log(preço);
//Até roda, mas na declaração de variáveis não é correto utilizar acentuação nos nomes, assim como não se deve começar com números ou usa palavas reservadas

